BUGS


FEATURES:
Часы с тем же названием и серийным номером в течении суток будут считаться продолжением одной сессии.

REFACTORING:
//Сделать функцию repaint() удаленной : repaint(object)

Date.prototype.getHoursWith0()

UPDATES:
Установить возможность запуска только одной копии проги.

Записывать-Сохранять разные файлы + (checkBox)

автоматически нажимать Стоп при простое (мышь, клава)

настройки Экспорта

цифровая подпись